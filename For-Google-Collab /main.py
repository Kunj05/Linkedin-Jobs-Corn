import os

# Set your LinkedIn token (LI_AT cookie) as an environment variable
os.environ['LI_AT_COOKIE'] = '-8NS56RP2iJM'  # Replace this with your actual LI_AT cookie value

# Run the script to start scraping job listings
!python python.py
